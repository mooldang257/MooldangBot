server {
    listen 80;
    server_name localhost;

    location /fonts/ {
        alias /usr/share/nginx/html/fonts/;
        include /etc/nginx/mime.types; # 표준 MIME 타입 사용
        add_header 'Access-Control-Allow-Origin' '*' always;
        autoindex on;
    }
}
