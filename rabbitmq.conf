# [RabbitMQ Configuration]
# ---------------------------------------------------------
# [v18.1] 경로 기반 도메인 연동을 위한 설정
# ---------------------------------------------------------

# 관리 UI의 기본 경로를 /rabbit 으로 변경 (CSS/JS 경로 포함)
management.path_prefix = /rabbit
