# [RabbitMQ Configuration]
# ---------------------------------------------------------
# [v18.1] 경로 기반 도메인 연동을 위한 설정
# ---------------------------------------------------------

# 관리 UI의 기본 경로를 /admin/rabbit 으로 변경 (보안 및 어드민 통합용)
management.path_prefix = /admin/rabbit
