# [오시리스의 함대] - FRONTEND UI LAYER
# 관리용 스튜디오 및 어드민 웹 서비스를 담당합니다.

services:
  studio:
    build:
      context: .
      dockerfile: ./MooldangBot.Studio/Dockerfile
    image: mooldang-studio:${VERSION_UI:-latest}
    container_name: mooldang-dev-studio
    restart: always
    deploy:
      resources:
        limits:
          memory: 256M
    environment:
      - NODE_ENV=development
      - ORIGIN=https://${BASE_DOMAIN}
      - INTERNAL_API_URL=http://app:8080
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.studio-dev.rule=(Host(`${BASE_DOMAIN}`)) && PathPrefix(`/`) && !PathPrefix(`/api`) && !PathPrefix(`/admin`) && !PathPrefix(`/overlay`) && !PathPrefix(`/fonts`)"
      - "traefik.http.routers.studio-dev.entrypoints=web"
      - "traefik.http.services.studio-dev.loadbalancer.server.port=3000"
      - "traefik.http.middlewares.dev-noindex.headers.customresponseheaders.X-Robots-Tag=noindex, nofollow, noarchive, nosnippet"
      - "traefik.http.routers.studio-dev.middlewares=cloudflare-real-ip@file,dev-noindex"
      - "traefik.docker.network=mooldang_dev_net"
      - "traefik.env=dev"
    networks:
      - mooldang_dev_net

  admin:
    build:
      context: .
      dockerfile: ./MooldangBot.Admin/Dockerfile
    image: mooldang-admin:${VERSION_UI:-latest}
    container_name: mooldang-dev-admin
    restart: always
    deploy:
      resources:
        limits:
          memory: 256M
    environment:
      - NODE_ENV=development
      - ORIGIN=https://${BASE_DOMAIN}
      - INTERNAL_API_URL=http://app:8080
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.admin-dev.rule=(Host(`${BASE_DOMAIN}`)) && PathPrefix(`/admin`)"
      - "traefik.http.routers.admin-dev.entrypoints=web"
      - "traefik.http.routers.admin-dev.middlewares=traefik-auth@file,cloudflare-real-ip@file"
      - "traefik.http.services.admin-dev.loadbalancer.server.port=3000"
      - "traefik.docker.network=mooldang_dev_net"
      - "traefik.env=dev"
    networks:
      - mooldang_dev_net

networks:
  mooldang_dev_net:
    external: true
